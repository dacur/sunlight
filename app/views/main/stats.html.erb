<div class = "container-fluid">
	<div class = "row">
		<div class = "col-md-6">

		<h2>Total raised by politicians in the US in 2012</h1>

		<%#=@top[0]["amount"] %>
		<%#=@top[1]["amount"] %>

			
		<h4><%= number_to_currency(@total) %> (top 10,000 campaigns by amount raised)<h4>
		<p></p>
		<h3>Top 10 campaigns by dollars raised (2012)</h3>

			<% y = 0 %>
			<% for y in 0..9 %>
				<p><%= (y+1).to_s + ". " + @top[y]["name"].to_s + " " + number_to_currency(@top[y]["amount"].to_i) %></p>
			<% end %>

			<h4><%= "Total for the Top 10: " + number_to_currency(@top10total).to_s %>


				
			<br />

			<%#= link_to "Clear", main_testing_path, method: "post" %>
	</div>
</div>
</div>